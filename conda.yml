name: iris-inference-env
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - azureml-defaults
      - azureml-ai-monitoring
      - scikit-learn
      - pandas
      - numpy
      - joblib